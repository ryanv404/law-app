import React from 'react'

const Header = () => {
  const style = {
    borderBottom: "1px solid black",
    textAlign: "center"
  }

  return (
    <div style={style}>Header</div>
  )
}

export default Header